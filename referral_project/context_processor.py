from reff_no_api.models import UserStructure,CustomUser
def referral_processor(request):
    # user=request.user
    # if user.is_authenticated:
    #     return {'user':user}
    # else:
        return{}